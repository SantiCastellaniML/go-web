package main

func main() {
	/*
		 	r := chi.NewRouter()

			h := handler.NewHandler() //y dentro de handler podría estar el storage de productos que yo tengo en mi ejercicio.
			//como el handle lo maneja esta variable, va a tener acceso a ese campo que es un slice de productos.

			r.Get("/", h.Get())
			r.Get("/users/{userID}", h.GetById())
			r.Get("/users", h.GetByQuery())

			http.ListenAndServe(":8080", r)
	*/
}
